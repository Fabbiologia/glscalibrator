% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gls_pipeline.R
\name{run_gls_pipeline_batch}
\alias{run_gls_pipeline_batch}
\title{Batch-run the full GLS pipeline over a directory of deployments}
\usage{
run_gls_pipeline_batch(
  root,
  config,
  output_dir = "probGLS_output",
  light_ext = c("lux", "lig"),
  ...,
  verbose = TRUE
)
}
\arguments{
\item{root}{Root directory searched recursively for deployments.}

\item{config}{A \code{\link{probgls_config}}.}

\item{output_dir}{Where to write per-deployment track CSVs and the summary.}

\item{light_ext}{Light-file extensions that mark a deployment folder.}

\item{...}{Passed to \code{\link{run_gls_pipeline}} (e.g. \code{colony},
\code{light_threshold}).}

\item{verbose}{Logical, print progress.}
}
\value{
A list of class \code{"gls_pipeline_batch"} with:
  \item{summary}{one row per deployment (id, status, n steps, lat/lon medians)}
  \item{results}{named list of \code{gls_pipeline} outputs (successes)}
  \item{log}{per-deployment status / error messages}
}
\description{
Auto-discovers deployments (sub-folders containing a light file), runs
\code{\link{run_gls_pipeline}} on each with robust per-deployment error
handling, writes per-deployment tracks, and returns a combined summary. This
is the movement-model counterpart to \code{\link{calibrate_gls_batch}}.
}
\examples{
\dontrun{
cfg <- read_probgls_config("config.yml")
batch <- run_gls_pipeline_batch("birds", cfg, output_dir = "results/probGLS")
batch$summary
}
}
