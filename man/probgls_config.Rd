% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probgls_config.R
\name{probgls_config}
\alias{probgls_config}
\alias{validate_probgls_config}
\alias{print.probgls_config}
\title{Build, validate and store a probGLS pipeline configuration}
\usage{
probgls_config(
  tagging_location = NULL,
  boundary_box = NULL,
  NOAA_OI_location = NULL,
  particle_number = 2000,
  iteration_number = 100,
  loess_quartile = NULL,
  tagging_date = NULL,
  retrieval_date = NULL,
  sunrise_sd = NULL,
  sunset_sd = NULL,
  tol = 0.08,
  range_solar = c(-7, -1),
  speed_dry = c(12, 6, 45),
  speed_wet = c(1, 1.3, 5),
  distance_method = "ellipsoid",
  sst_sd = 0.5,
  max_sst_diff = 3,
  ice_conc_cutoff = 1,
  east_west_comp = TRUE,
  land_mask = TRUE,
  med_sea = FALSE,
  black_sea = FALSE,
  baltic_sea = FALSE,
  caspian_sea = FALSE,
  wetdry_resolution = 1,
  backward = FALSE,
  preset = c("seabird")
)

validate_probgls_config(x, require_runtime = TRUE)

\method{print}{probgls_config}(x, ...)
}
\arguments{
\item{tagging_location}{Numeric length-2 \code{c(lon, lat)} of the
deployment (colony / capture) location. Required.}

\item{boundary_box}{Numeric length-4 \code{c(xmin, xmax, ymin, ymax)}
spatial bounds of plausible movement, in decimal degrees. Required.}

\item{NOAA_OI_location}{Character path to the folder holding the
environmental fields (NOAA OI SST, sea-ice, land mask) prepared by
\code{\link{get_environmental_data}}. Required to run the particle model.}

\item{particle_number, iteration_number}{Integer particle and iteration counts.}

\item{loess_quartile}{Numeric loess light-smoothing quartiles, or \code{NULL}.}

\item{tagging_date, retrieval_date}{Deployment start / end
(\code{POSIXct}/\code{Date}); \code{NULL} = derive from the twilights.}

\item{sunrise_sd, sunset_sd}{Numeric length-3 twilight-error terms, or
\code{NULL} to estimate them from the calibration twilights.}

\item{tol}{Numeric tolerance on solar declination.}

\item{range_solar}{Numeric length-2 min/max solar angle window.}

\item{speed_dry, speed_wet}{Numeric length-3 \code{c(optimal, sd, max)} travel
speeds (m/s) while the logger is dry / wet.}

\item{distance_method}{Character distance model, e.g. \code{"ellipsoid"}.}

\item{sst_sd}{Numeric SST error (deg C).}

\item{max_sst_diff}{Numeric maximum allowed \code{|tag SST - satellite SST|}.}

\item{ice_conc_cutoff}{Numeric sea-ice concentration cutoff (percent).}

\item{east_west_comp}{Logical, apply east-west (longitude) compensation.}

\item{land_mask}{Logical, restrict positions to sea (\code{TRUE}) or allow
land (\code{FALSE}); \code{NULL} passes through unchanged.}

\item{med_sea, black_sea, baltic_sea, caspian_sea}{Logical, treat these enclosed
seas as land.}

\item{wetdry_resolution}{Numeric conductivity sampling resolution (seconds).}

\item{backward}{Logical, run the algorithm from end to start.}

\item{preset}{Optional character naming a starting preset. Currently
\code{"seabird"} (the documented general defaults). Explicit arguments
always override the preset.}

\item{x}{A \code{probgls_config} object.}

\item{require_runtime}{Logical; if \code{TRUE}, also require the fields that
are needed to actually launch the particle model
(\code{tagging.location}, \code{boundary.box}, \code{NOAA.OI.location}).}

\item{...}{Ignored.}
}
\value{
An object of class \code{"probgls_config"}: a named list whose names
  are exactly the \code{probGLS::prob_algorithm()} argument names (dotted).

\code{validate_probgls_config()} returns \code{x} invisibly on
  success and stops with an informative error otherwise.
}
\description{
The movement-model stage of the pipeline is driven entirely by a
configuration object so that no biology is hard-coded and the package stays
taxon-agnostic. The object mirrors, one-to-one, the arguments of
\code{probGLS::prob_algorithm()} (the 29 components documented in the
project spec sheet). Arguments that are derived automatically by the pipeline
(\code{trn}, \code{sensor}, \code{act}, \code{tagging.date},
\code{retrieval.date}, \code{sunrise.sd}, \code{sunset.sd}) may be left
\code{NULL} and are filled in at run time.
}
\details{
Defaults follow the general-purpose values recommended for probGLS and are
\emph{not} specific to any species. Override them per deployment.
}
\examples{
cfg <- probgls_config(
  tagging_location = c(-115.174, 27.852),
  boundary_box     = c(-140, -80, 0, 55),
  NOAA_OI_location = tempdir()
)
cfg[["speed.wet"]]

}
\seealso{
\code{\link{run_probgls}}, \code{\link{run_gls_pipeline}},
  \code{\link{read_probgls_config}}, \code{\link{write_probgls_config}}
}
