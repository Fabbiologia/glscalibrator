% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gls_pipeline.R
\name{run_gls_pipeline}
\alias{run_gls_pipeline}
\title{Run the full GLS pipeline for a single deployment}
\usage{
run_gls_pipeline(
  path,
  config,
  colony = NULL,
  type = "auto",
  light_threshold = 2,
  temp_range = c(-2, 32),
  run_model = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{path}{Directory (or light-file path) for one deployment; passed to
\code{\link{read_gls}}.}

\item{config}{A \code{\link{probgls_config}} (biology + environment settings).
If its \code{tagging.location} is \code{NULL} you must supply \code{colony}.}

\item{colony}{Optional \code{c(lon, lat)} to set/override the tagging location.}

\item{type}{Logger type for \code{\link{read_gls}} (\code{"auto"} by default).}

\item{light_threshold}{Light threshold (lux) for twilight detection.}

\item{temp_range}{Plausible SST range passed to \code{\link{deduce_sst}}.}

\item{run_model}{Logical; if \code{FALSE}, stop after preparing inputs
(\code{trn}, \code{sensor}) without calling the particle model.}

\item{verbose}{Logical, print progress.}
}
\value{
A list of class \code{"gls_pipeline"} with \code{id}, \code{deployment},
  \code{twilights}, \code{trn}, \code{sensor}, and (if \code{run_model})
  \code{result} (a \code{\link{run_probgls}} output).
}
\description{
End-to-end: read the raw logger, detect and filter twilights, deduce in-situ
SST, and reconstruct a most-probable, SST-constrained track with the particle
model. This is the single-deployment engine behind
\code{\link{run_gls_pipeline_batch}}.
}
\examples{
\dontrun{
cfg <- probgls_config(tagging_location = c(-115.174, 27.852),
                      boundary_box = c(-140, -80, 0, 55),
                      NOAA_OI_location = "results/oisst")
out <- run_gls_pipeline("birds/BW148", cfg)
head(out$result$track)
}
}
\seealso{
\code{\link{run_gls_pipeline_batch}}, \code{\link{run_probgls}}
}
